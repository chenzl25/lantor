# Web attachment delivery and upload limits

`GET /api/attachments/{id}` serves the immutable file identified by the attachment UUID. Successful representations have `Cache-Control: private, max-age=31536000, immutable`, a quoted UUID `ETag`, and `Accept-Ranges: bytes`. `If-None-Match` supports weak/list/wildcard matches and returns 304. Existing MIME inference, `nosniff`, filename handling, and forced download for active content are preserved. The private directive permits the browser cache while excluding shared proxy caches. Browser HTTP caching applies independently of the service worker's network-only API policy.

The body uses a Tokio file and a 64KiB `ReaderStream`, bounded by the requested length. Closed (`bytes=0-99`), open-ended (`bytes=100-`), and suffix (`bytes=-100`) ranges return 206 with `Content-Range`; unsatisfiable or malformed single byte ranges return 416 with `bytes */<size>` and `no-store`. Unsupported range units and multiple ranges return the whole file. A stale/weak/date `If-Range` also returns the whole file; the exact strong ETag permits a range. HEAD returns full metadata without reading the body and ignores Range.

Multipart uploads stream through `field.chunk()` to a UUID file under `attachments/.tmp/`. The request body yields between 64KiB slices because Multer otherwise drains all immediately-ready chunks into its own buffer before returning a field chunk. Backpressure then follows the file writes. Request JSON metadata is capped at 1MiB. A known **total multipart Content-Length above 64MiB** is rejected with 413 before body polling (including before `100 Continue`). This total includes multipart overhead and all files. For requests without Content-Length, each file accepts at most 64MiB, and the existing 128MiB total body cap remains. This distinction follows the task's early rejection rule; a known-length file of exactly 64MiB does not fit with its envelope.

The internal `StagedAttachment` is skipped by JSON deserialization; clients cannot supply server file paths. After validation, the storage transaction renames the staged file on the same filesystem and inserts the unchanged attachment row. Drop guards remove staged files after malformed requests, transport errors, cancellation, and validation failures; the existing pending-write guard removes final files if the database transaction fails. Create/rename are short synchronous filesystem operations to avoid cancellation racing a detached operation; actual file contents use asynchronous I/O. Hard process termination may leave temporary files. JSON/Tauri byte-array uploads retain their existing contract; this streaming change is for web multipart uploads.

Thumbnails were optional in task #106's final handoff and are deferred. Original image URLs and frontend rendering are unchanged.

## Verification

- `cargo test --manifest-path src-tauri/Cargo.toml`: range bodies/validators/HEAD/active-content disposition, 64MiB boundary, never-ready body early rejection, bounded reads for a ready 100MiB stream, invalid metadata, disconnected/cancelled requests, and database rollback cleanup, plus the full existing suite.
- `npm run test:attachments`: opt-in fresh isolated database and real Axum server, binary multipart persistence, native Chromium image rendering/CDP cache events, direct local `curl --noproxy '*' -r 0-99`, and two 100MiB upload attempts with/without Content-Length. The Node client generates 64KiB chunks and samples the server's RSS using `ps` about every 10ms. No deployed supervisor or live database is involved.
- To collect JSON, set `LANTOR_ATTACHMENT_EVIDENCE_DIR` to an existing output directory. `LANTOR_ATTACHMENT_BASELINE=1` makes the same benchmark observational for a checkout before this change (copy the fixture module and browser script into that checkout).
- `npm test`, `npm run build`, `npm run test:web-sync`, and `git diff --check` cover frontend compatibility and integration.
